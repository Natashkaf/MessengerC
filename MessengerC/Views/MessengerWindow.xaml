<Window x:Class="MessengerApp.MessengerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:messengerApp="clr-namespace:MessengerApp"
        xmlns:converters="clr-namespace:MessengerApp.Converters"
        Title="Messenger" 
        Height="700" 
        Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="White"
        Closing="Window_Closing">
    
    <Window.Resources>
        <!-- Стили для сообщений -->
        <Style x:Key="MyMessageStyle" TargetType="Border">
            <Setter Property="Background" Value="#0088CC"/>
            <Setter Property="CornerRadius" Value="18 4 18 18"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="60,5,15,5"/> 
            <Setter Property="MaxWidth" Value="400"/>
        </Style>
    
        <Style x:Key="TheirMessageStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="4 18 18 18"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Margin" Value="15,5,60,5"/>
            <Setter Property="MaxWidth" Value="400"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        
        <!-- Конвертер времени -->
        <messengerApp:DateTimeToStringConverter x:Key="TimeConverter"/>
        <messengerApp:MessageStyleConverter x:Key="MessageStyleConverter"/>
        <messengerApp:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <messengerApp:IntToVisibilityConverter x:Key="IntToVisibilityConverter"/>
        <messengerApp:BoolToItalicConverter x:Key="BoolToItalicConverter"/>

        <messengerApp:FileBackgroundConverter x:Key="FileBackgroundConverter"/>
        <messengerApp:DownloadButtonBorderConverter x:Key="DownloadButtonBorderConverter"/>
        <messengerApp:DownloadButtonTextConverter x:Key="DownloadButtonTextConverter"/>
    
        <!-- Конвертер для иконки файла -->
        <messengerApp:FileIconConverter x:Key="FileIconConverter"/>
    
        <!-- Конвертер для размера файла -->
        <messengerApp:FileSizeConverter x:Key="FileSizeConverter"/>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <!-- Левая колонка: список контактов -->
            <ColumnDefinition Width="350"/>
            <!-- Правая колонка: чат -->
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Grid Grid.Column="0" Background="#F8F9FA">
            <Grid.RowDefinitions>

                <RowDefinition Height="Auto"/>    
                <RowDefinition Height="Auto"/> 
                <RowDefinition Height="*"/>     
            </Grid.RowDefinitions>
            
            <Border Grid.Row="0" Padding="15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Аватар пользователя -->
                    <Border Grid.Column="0" Width="45" Height="45" CornerRadius="22.5"
                            BorderBrush="#0088CC" BorderThickness="2"
                            Background="Transparent"
                            Cursor="Hand"
                            MouseLeftButtonDown="UserAvatar_MouseLeftButtonDown">
                        <Image x:Name="UserAvatarImage" Stretch="UniformToFill">
                            <Image.Clip>
                                <EllipseGeometry Center="22.5,22.5" 
                                                 RadiusX="22.5" RadiusY="22.5"/>
                            </Image.Clip>
                        </Image>
                    </Border>
                    
                    <!-- Имя пользователя и статус -->
                    <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                        <TextBlock x:Name="UserNameText" Text="Загрузка..." 
                                   Foreground="#333" FontSize="16" FontWeight="500"/>
                        <TextBlock x:Name="UserStatusText" Text="онлайн" 
                                   Foreground="#0088CC" FontSize="13"/>
                    </StackPanel>
                    <Button Grid.Column="2" 
                            Width="40" Height="40"
                            Background="Transparent" 
                            Cursor="Hand"
                            Click="SearchButton_Click">
                        <TextBlock Text="?" 
                                   Foreground="#333" FontSize="18"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                    
                    <!-- Кнопка меню -->
                    <Border Grid.Column="3" Width="40" Height="40" CornerRadius="20"
                            Background="Transparent" Cursor="Hand"
                            MouseLeftButtonDown="MenuButton_MouseLeftButtonDown">
                        <TextBlock Text="⋮" 
                                   Foreground="#333" FontSize="20" FontWeight="Bold"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>
            
            <!-- Поиск контактов -->
            <Border Grid.Row="1" Background="White" Padding="10">
                <TextBox x:Name="SearchTextBox" 
                         VerticalAlignment="Center"
                         Padding="10,8" FontSize="14"
                         Text="Поиск контактов..."
                         TextChanged="SearchTextBox_TextChanged">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Border Background="#F0F2F5" CornerRadius="10">
                                            <Grid>
                                                <TextBlock Text="?" VerticalAlignment="Center" 
                                                           HorizontalAlignment="Left" Margin="10,0,0,0"
                                                           Foreground="#666"/>
                                                <ScrollViewer x:Name="PART_ContentHost" 
                                                              Margin="30,0,10,0"
                                                              VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </Border>
            
            <!-- Список контактов -->
            <Border Grid.Row="2" Background="White">
                <ListView x:Name="ContactsList" Background="White" BorderThickness="0"
                          SelectionChanged="ContactsList_SelectionChanged">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="15,10" BorderBrush="#F0F0F0" BorderThickness="0,0,0,1"
                                    MouseLeftButtonDown="ContactItem_MouseLeftButtonDown">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Аватар контакта -->
                                    <Border Grid.Column="0" Width="45" Height="45" 
                                            CornerRadius="22.5" Background="#E0E0E0">
                                        <TextBlock Text="{Binding Initials}" 
                                                   VerticalAlignment="Center" HorizontalAlignment="Center"
                                                   FontSize="16" FontWeight="500" Foreground="#666"/>
                                    </Border>
                                    
                                    <!-- Имя и последнее сообщение -->
                                    <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontSize="15" FontWeight="500"/>
                                        <TextBlock Text="{Binding LastMessage}" 
                                                   Foreground="#666" FontSize="13"
                                                   TextTrimming="CharacterEllipsis" MaxWidth="150"/>
                                    </StackPanel>
                                    
                                    <!-- Время и статус -->
                                    <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Time}" Foreground="#999" FontSize="12"/>
                                        <Border Width="10" Height="10" CornerRadius="5" 
                                                Background="{Binding StatusColor}" 
                                                Margin="0,5,0,0" HorizontalAlignment="Right"/>
                                        
                                        <!-- Badge для непрочитанных сообщений -->
                                        <Border x:Name="UnreadBadge" 
                                                Background="#0088CC" 
                                                CornerRadius="10"
                                                Width="20" Height="20"
                                                Margin="0,5,0,0"
                                                HorizontalAlignment="Right"
                                                Visibility="{Binding UnreadCount, Converter={StaticResource IntToVisibilityConverter}}">
                                            <TextBlock Text="{Binding UnreadCount}" 
                                                       Foreground="White" 
                                                       FontSize="11"
                                                       FontWeight="Bold"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                                        </Border>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>
        </Grid>
        

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Заголовок чата -->
            <Border x:Name="ChatHeader" Grid.Row="0" Background="White" BorderBrush="#E0E0E0" 
                    BorderThickness="0,0,0,1" Height="70" Visibility="Collapsed">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Кнопка назад -->
                    <Button x:Name="BackButton" Grid.Column="0" Content="‹"
                            Background="Transparent" BorderThickness="0"
                            FontSize="24" FontWeight="Bold" Width="50"
                            Click="BackButton_Click"/>
                    
                    <!-- Информация о собеседнике -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="15,0,0,0">
                        <Border Width="40" Height="40" CornerRadius="20" Background="#E0E0E0"
                                VerticalAlignment="Center">
                            <TextBlock x:Name="ChatContactInitials" Text="" 
                                       VerticalAlignment="Center" HorizontalAlignment="Center"
                                       FontSize="14" FontWeight="500"/>
                        </Border>
                        <StackPanel Margin="15,0,0,0" VerticalAlignment="Center">
                            <TextBlock x:Name="ChatContactName" Text="" 
                                       FontSize="16" FontWeight="500"/>
                            <TextBlock x:Name="ChatContactStatus" Text="" 
                                       Foreground="#0088CC" FontSize="13"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <!-- Кнопки действий в чате -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,15,0">
                        
                        <Button Content="⋮" Background="Transparent" BorderThickness="0"
                                FontSize="20" FontWeight="Bold" Width="40" Margin="5,0,0,0"
                                ToolTip="Дополнительно"
                                Click="ChatMenuButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Область сообщений -->
            <Border Grid.Row="1" Background="#F0F2F5">
                <Grid>
<ScrollViewer x:Name="MessagesScrollViewer" 
              VerticalScrollBarVisibility="Auto"
              ScrollChanged="MessagesScrollViewer_ScrollChanged">
    <ItemsControl x:Name="MessagesList">
<ItemsControl.ItemTemplate>
    <DataTemplate>
        <Grid Margin="0,5,0,5">
            <!-- Дата разделитель -->
            <Border x:Name="DateSeparator" 
                    Background="#E0E0E0" 
                    CornerRadius="10" 
                    Padding="10,5"
                    HorizontalAlignment="Center"
                    Margin="0,10"
                    Visibility="{Binding IsDateSeparator, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock Text="{Binding Date}" 
                           Foreground="#666" 
                           FontSize="12"/>
            </Border>
            
            <!-- Сообщение -->
            <Border x:Name="MessageContainer"
                    MouseRightButtonDown="MessageContainer_MouseRightButtonDown"
                    Padding="12,8"
                    CornerRadius="12"
                    MaxWidth="400"
                    Margin="0,5,0,5"
                    Visibility="{Binding IsMessage, Converter={StaticResource BoolToVisibilityConverter}}">
                
                <!-- Стиль с триггерами -->
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="HorizontalAlignment" Value="Left"/>
                        <Setter Property="Margin" Value="15,5,60,5"/>
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="BorderBrush" Value="#E0E0E0"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        
                        <Style.Triggers>

                            <DataTrigger Binding="{Binding IsMyMessage}" Value="True">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="Margin" Value="60,5,15,5"/>
                                <Setter Property="Background" Value="#0088CC"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="CornerRadius" Value="18 4 18 18"/>
                            </DataTrigger>
                            

                            <DataTrigger Binding="{Binding IsMyMessage}" Value="False">
                                <Setter Property="HorizontalAlignment" Value="Left"/>
                                <Setter Property="Margin" Value="15,5,60,5"/>
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="CornerRadius" Value="4 18 18 18"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Содержимое сообщения -->
                    <StackPanel Grid.Row="0" 
                                Orientation="Vertical"
                                Margin="0,0,0,5">
                        
                        <!-- Если это файл -->
                        <Border x:Name="FileContainer"
                                Visibility="{Binding HasAttachment, Converter={StaticResource BoolToVisibilityConverter}}"
                                Background="{Binding IsMyMessage, Converter={StaticResource FileBackgroundConverter}}"
                                CornerRadius="8"
                                Padding="10"
                                Margin="0,0,0,8">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding FileIcon}"
                                           FontSize="20"
                                           Margin="0,0,10,0"
                                           VerticalAlignment="Center"/>
                                
                                <StackPanel>
                                    <TextBlock Text="{Binding FileName}" 
                                               FontWeight="Bold"
                                               TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding FileSizeText}"
                                               FontSize="11"
                                               Opacity="0.8"/>
                                    
                                    <!-- Кнопка скачивания -->
                                    <Button Content="Скачать"
                                            Margin="0,8,0,0"
                                            Padding="6,3"
                                            Background="Transparent"
                                            BorderBrush="{Binding IsMyMessage, Converter={StaticResource DownloadButtonBorderConverter}}"
                                            Foreground="{Binding IsMyMessage, Converter={StaticResource DownloadButtonTextConverter}}"
                                            FontSize="11"
                                            HorizontalAlignment="Left"
                                            Click="DownloadFileButton_Click">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Cursor" Value="Hand"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#40000000"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                        
                        <!-- Текст сообщения (отображается всегда, даже для файлов) -->
                        <TextBlock Text="{Binding DisplayText}" 
                                   TextWrapping="Wrap"
                                   FontSize="14"
                                   FontFamily="Segoe UI"
                                   FontStyle="{Binding IsDeleted, Converter={StaticResource BoolToItalicConverter}}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="Black"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMyMessage}" Value="True">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsDeleted}" Value="True">
                                            <Setter Property="Foreground" Value="Gray"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                    
                    <!-- Время и статус отправки -->
                    <StackPanel Grid.Row="1" 
                                Orientation="Horizontal" 
                                HorizontalAlignment="Right">
                        <TextBlock Text="{Binding Time}" 
                                   FontSize="11"
                                   Margin="0,0,5,0"
                                   Opacity="0.8">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMyMessage}" Value="True">
                                            <Setter Property="Foreground" Value="#E6F2FF"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsMyMessage}" Value="False">
                                            <Setter Property="Foreground" Value="#666666"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        
                        <!-- Метка "Изменено" -->
                        <TextBlock Text=" (изм.)" 
                                   FontSize="9"
                                   Opacity="0.6"
                                   Margin="0,0,5,0"
                                   Visibility="{Binding IsEdited, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMyMessage}" Value="True">
                                            <Setter Property="Foreground" Value="#E6F2FF"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsMyMessage}" Value="False">
                                            <Setter Property="Foreground" Value="#666666"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        
                        <!-- Статус отправки -->
                        <TextBlock Text="{Binding StatusIcon}" 
                                   FontSize="11"
                                   Opacity="0.8">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="{Binding IsMyMessage, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMyMessage}" Value="True">
                                            <Setter Property="Foreground" Value="#E6F2FF"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </DataTemplate>
</ItemsControl.ItemTemplate>
    </ItemsControl>
</ScrollViewer>
                    
                    <!-- Индикатор загрузки сообщений -->
                    <Border x:Name="LoadingIndicator" 
                            Background="#80000000" 
                            Visibility="Collapsed"
                            VerticalAlignment="Top"
                            HorizontalAlignment="Center"
                            Margin="0,10,0,0"
                            Padding="10"
                            CornerRadius="5">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⏳" FontSize="16" Margin="0,0,10,0"/>
                            <TextBlock Text="Загрузка сообщений..." 
                                       Foreground="White" 
                                       FontSize="14"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
            
            <!-- Панель ввода сообщения -->
            <Border x:Name="MessageInputPanel" Grid.Row="2" Background="White" BorderBrush="#E0E0E0" 
                    BorderThickness="0,1,0,0" Padding="15" Visibility="Collapsed">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Кнопки прикрепления -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Content="📎" Background="Transparent" BorderThickness="0"
                                FontSize="18" Width="40" ToolTip="Прикрепить файл"
                                Click="AttachFileButton_Click"/>

                    </StackPanel>
                    
                    <!-- Поле ввода -->
                    <Border Grid.Column="1" Background="#F0F2F5" CornerRadius="20" 
                            Margin="10,0,10,0" Padding="5">
                        <Grid>
                            <TextBox x:Name="MessageTextBox" 
                                     BorderThickness="0" Background="Transparent"
                                     VerticalAlignment="Center" Padding="15,10"
                                     FontSize="14" AcceptsReturn="True"
                                     TextWrapping="Wrap" MinHeight="40"
                                     VerticalScrollBarVisibility="Auto"
                                     TextChanged="MessageTextBox_TextChanged"
                                     KeyDown="MessageTextBox_KeyDown">
                                <TextBox.InputBindings>
                                    <KeyBinding Key="Enter" Command="{Binding SendMessageCommand}"/>
                                </TextBox.InputBindings>
                            </TextBox>
                            
                            <!-- Индикатор набора текста -->
                            <Border x:Name="TypingIndicator" 
                                    Background="#E0E0E0" 
                                    CornerRadius="10"
                                    Padding="10,5"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Top"
                                    Margin="10,5"
                                    Visibility="Collapsed">
                                <TextBlock x:Name="TypingText" 
                                           Text="" 
                                           FontSize="12" 
                                           Foreground="#666"/>
                            </Border>
                        </Grid>
                    </Border>
                    
                    <!-- Кнопка отправки -->
                    <Button Grid.Column="2" x:Name="SendMessageButton" Content="➤" 
                            Background="#0088CC" Foreground="White"
                            BorderThickness="0" FontSize="16" FontWeight="Bold"
                            Width="45" Height="45"  
                            Click="SendButton_Click"
                            IsEnabled="False"/>
                </Grid>
            </Border>
            
            <!-- Подсказка, если чат не выбран -->
            <Border Grid.Row="1" x:Name="NoChatSelectedPanel" 
                    Background="#F0F2F5" Visibility="Visible">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    
                    <TextBlock Text="Выберите контакт для начала общения" 
                               FontSize="18" FontWeight="500" TextAlignment="Center"/>
                    <TextBlock Text="Ваши сообщения будут отображаться здесь" 
                               Foreground="#666" Margin="0,10,0,0" TextAlignment="Center"/>
                    <TextBlock Text="Не хватает стикера как в тг " 
                               Foreground="#666" Margin="0,10,0,0" TextAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- Окно предпросмотра файла -->
        <Border x:Name="FilePreviewPanel" 
                Grid.ColumnSpan="2"
                Background="#80000000"
                Visibility="Collapsed"
                MouseLeftButtonDown="FilePreviewPanel_MouseLeftButtonDown">
            <Border Background="White" 
                    Width="400" 
                    Height="300"
                    CornerRadius="10"
                    Padding="20"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="Прикрепленный файл" 
                               FontSize="16" 
                               FontWeight="Bold"
                               Margin="0,0,0,10"/>
                    
                    <StackPanel Grid.Row="1" 
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center">
                        <TextBlock x:Name="FileNameText" 
                                   Text="" 
                                   FontSize="14"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   Margin="0,0,0,10"/>
                        <TextBlock x:Name="FileSizeText" 
                                   Text="" 
                                   FontSize="12"
                                   Foreground="#666"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Row="2" 
                                Orientation="Horizontal"
                                HorizontalAlignment="Right">
                        <Button Content="Отмена" 
                                Background="Transparent"
                                Foreground="#666"
                                Click="CancelFileButton_Click"
                                Margin="0,0,10,0"/>
                        <Button Content="Отправить" 
                                Background="#0088CC"
                                Foreground="White"
                                Click="SendFileButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Border>
    </Grid>
</Window>