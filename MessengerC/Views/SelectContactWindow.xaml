<Window x:Class="MessengerApp.SelectContactWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Выберите контакт" 
        Height="500" Width="400"
        WindowStartupLocation="CenterOwner"
        Background="White">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#0088CC" Padding="15">
            <TextBlock Text="Выберите контакт для пересылки" 
                       Foreground="White" FontSize="16" FontWeight="Bold"/>
        </Border>
        
        <!-- Список контактов -->
        <ListView x:Name="ContactsList" Grid.Row="1" 
                  Background="White" BorderThickness="0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Padding="15,10" BorderBrush="#F0F0F0" BorderThickness="0,0,0,1"
                            MouseLeftButtonDown="ContactItem_MouseLeftButtonDown"
                            Cursor="Hand">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Аватар -->
                            <Border Grid.Column="0" Width="45" Height="45" 
                                    CornerRadius="22.5" Background="#E0E0E0">
                                <TextBlock Text="{Binding Initials}" 
                                           VerticalAlignment="Center" HorizontalAlignment="Center"
                                           FontSize="16" FontWeight="500" Foreground="#666"/>
                            </Border>
                            
                            <!-- Имя -->
                            <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" FontSize="15" FontWeight="500"/>
                                <TextBlock Text="{Binding StatusText}" 
                                           Foreground="#666" FontSize="13"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <!-- Кнопки -->
        <Border Grid.Row="2" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" >
                <Button Content="Отмена" Width="80" Margin="0,0,10,0"
                        Click="CancelButton_Click"/>
                <Button Content="Выбрать" Width="80" Background="#0088CC"
                        Foreground="White" Click="SelectButton_Click"/>
            </StackPanel>
        </Border>
        
        <!-- Сообщение если нет контактов -->
        <Border Grid.Row="1" x:Name="NoContactsPanel" 
                Visibility="Collapsed" Background="White">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="👤" FontSize="40" Margin="0,0,0,15"/>
                <TextBlock Text="Нет доступных контактов" 
                           FontSize="16" FontWeight="500"/>
                <TextBlock Text="Добавьте контакты для пересылки" 
                           Foreground="#666" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>